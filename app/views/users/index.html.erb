<div class="container-fluid d-flex flex-column align-items-center">
  <h1>Users#index</h1>
  <p> Hello Dear Patient! </p>
  <p>Your authorization number is <%= current_user.phone%></p>           
  <p>There are <%= Doctor.count %> doctor(s) in the system</p>
  <p>There are <%= User.count %> patient(s) in the system</p>   
       
  <%= button_to "Patient Signout", destroy_user_session_path, 'method': :delete, class: "btn btn-warning text-black" %>
</div>  
